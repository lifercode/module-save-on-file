services:
  ai-llama:
    build: .
    ports:
      - "5555:5555"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - my-network

networks:
  my-network:
    driver: bridge